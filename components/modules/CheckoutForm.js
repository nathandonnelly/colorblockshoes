import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const CheckoutForm = (props) => {
  return (
    <View>
      <Text>CheckoutForm</Text>
    </View>
  )
}

export default CheckoutForm

const styles = StyleSheet.create({})